{
  "attributes": {
    "kicker": { "type": "string", "default": "Venue" },
    "title": { "type": "string", "default": "{{venue}}" },
    "text": { "type": "string", "default": "The conference venue in {{city}} hosts all CEEDUCON days and is easy to reach, fully accessible and built for a multi-room programme." },
    "buttonText": { "type": "string", "default": "Practical information" },
    "buttonUrl": { "type": "string", "default": "/practical-information/" },
    "panelLabel": { "type": "string", "default": "Getting there" },
    "panelTitle": { "type": "string", "default": "Metro B · Českomoravská" },
    "panelText": { "type": "string", "default": "Check current public transport connections, nearby railway stations and the most convenient route before travelling to {{venue}}." },
    "paper": { "type": "boolean", "default": true }
  }
}
