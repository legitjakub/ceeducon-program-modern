# ACF JSON

This directory is the local JSON save/load path for ACF field groups used by the CEEDUCON theme.

The theme registers the `CEEDUCON Content` options screen and reads ACF option fields first when ACF Pro is active. Concrete content values stay in the WordPress database. Only field structure should be saved here.

